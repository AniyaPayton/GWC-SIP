void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:

}

void loop(1) {
  digitalWrite(13, HIGH); // sets the digital pin 13 on (dot)
  delay(1000);            // waits for a second
  digitalWrite(13, LOW);  // sets the digital pin 13 off (pause)
  delay(1000);            // waits for a second
  digitalWrite(13, HIGH);  // sets the digital pin 13 off (dash)
  delay(3000);            // waits for a second 
  digitalWrite(13, LOW);  // sets the digital pin 13 off (pause)
  delay(1000);
  digitalWrite(13, HIGH);  // sets the digital pin 13 off (dash)
  delay(3000);
  digitalWrite(13, LOW);  // sets the digital pin 13 off (pause)
  delay(1000);
  digitalWrite(13, HIGH);  // sets the digital pin 13 off (dash)
  delay(3000);
}
void loop(1) {
  digitalWrite(13, HIGH); // sets the digital pin 13 on (dot)
  delay(1000);            // waits for a second
  digitalWrite(13, LOW);  // sets the digital pin 13 off (pause)
  delay(1000);            // waits for a second
